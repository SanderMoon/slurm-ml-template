# Project environment — override Makefile defaults per system
# This file is git-ignored.
PROJECT_SLUG={{ project_slug }}
REMOTE_HOST={{ remote_host }}
NAS_BASE={{ nas_base_path }}
NAS_MOUNT={{ nas_mount_path }}
TIER={{ default_tier }}
QOS={{ default_qos }}
DOCKER_IMAGE={{ docker_registry }}/{{ docker_namespace }}/{{ project_slug }}:latest
{% if slurm_mail_domain %}# SLURM_MAIL=your.email@{{ slurm_mail_domain }}  # uncomment for job failure notifications{% endif %}
