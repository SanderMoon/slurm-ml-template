"""Models for {{ project_name }}.

Define your model architectures here. This is the publishable part of the
package — keep dependencies minimal (torch only). Task modules in
``experiments/tasks/`` import from here.

Example:
    from {{ project_slug }}.models import ExampleModel
"""

from {{ project_slug }}.models.example import ExampleModel

__all__ = ["ExampleModel"]
